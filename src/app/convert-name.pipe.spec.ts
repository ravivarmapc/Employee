import { ConvertNamePipe } from './convert-name.pipe';

describe('ConvertNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertNamePipe();
    expect(pipe).toBeTruthy();
  });
});
